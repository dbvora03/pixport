# A file just to keep track of development urls

### Django order service

`localhost:8000/api/v1/products` (GET): gets all products
`localhost:8000/api/v1/produts` (POST): add products

### Admin Service
